{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.command // \"\"' | grep -q 'git commit' && { ruff check --fix --quiet *.py 2>/dev/null; ruff format --quiet *.py 2>/dev/null; git add -u -- '*.py' 2>/dev/null; true; } || true",
            "timeout": 30,
            "statusMessage": "Running ruff..."
          }
        ]
      }
    ]
  }
}
